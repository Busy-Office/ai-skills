# Gauntlet rounds — sharpen-intent

One row per round, appended, never rewritten. Artifact cells are repo-relative
so a reader can open what was graded. Budget: three rounds; if not PASS by
round 3, stop and report the gap rather than lower the bar.

Grading an intent review and draft against [`BAR.md`](BAR.md), class **I — intent review**, with the shared
critic at [`evals/CRITIC.md`](../../../../evals/CRITIC.md) and the instrument
at `evals/bar-check.mjs`.

| round | date | artifact | target | critic | verdict | fixes taken | tokens |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-10 | `rounds/r1-drifted-INTENT-REVIEW.md` | `fixtures/drifted` (public fixture) | not yet dispatched — this run's worker was not permitted to spawn a fresh-context subagent critic | instrument-only checkpoint, superseded by the row below | one artifact-wording fix (generic `LOOPS.md`/`SKILL.md` mentions in the lede read as unresolvable citations by M13; reworded to avoid naming files absent from the target) | build: not measured — critic round outstanding |
| 1 | 2026-09-10 | `rounds/r1-drifted-INTENT-REVIEW.md` | `fixtures/drifted` (public fixture) | fresh-context subagent, blind, per `evals/CRITIC.md` | **FAIL** — mechanical instrument 13/13 PASS, but 4 judged criteria FAIL: (S1) draft's "The change:" line invents "the same day, without opening a ticket" — grepped `intent.md`/`CLAUDE.md`/`docs/BACKLOG.md`, zero hits, not sourced or marked TO DECIDE; (C2) headline measure ("under 10 minutes by Q3") is a verbatim restatement of Objective 2's measure, not a distinct signal; (C5) do-next #1 only points `CLAUDE.md` at the still-drifted `intent.md` — no step writes the sharpened draft's content into it, so following do-next as written leaves the rules file pointed at unsharpened text; (S7/C1) falsifier and key-focus are both bare `TO DECIDE` with no attempted derivation and no "until" clause, despite the scorecard treating the draft as the deliverable | none yet — fixes below target the skill's draft-template guidance, not this artifact | critic subagent: 78,709 tokens / 22 tool uses / 349s |
| 2 | 2026-09-10 | `rounds/r2-drifted-INTENT-REVIEW.md` (rebuilt fresh from the fixture, not hand-patched) | `fixtures/drifted` (public fixture) | fresh-context subagent, blind, dispatched directly by the parent session, evals/CRITIC.md | **PASS** — mechanical instrument 13/13; all 10 shared and 5 of 6 class-I criteria PASS (6th, "already steers" verdict, N/A — this artifact's verdict is "decorative"); each round-1 failure independently re-verified as genuinely fixed, not cosmetic: "self-serve" traces to intent.md:5 as paraphrase, not invention; headline measure ("no engineer at all," an event) is explicitly distinguished from Objective 2's own 10-minute measure; do-next #1 now writes the draft's content into `intent.md` directly, not just a pointer; falsifier and key-focus both carry attempted derivations, flagged where unsupported rather than left bare | fix from round 1 applied to `references/report-template.md` (require sourced clauses, a headline measure distinct from any objective's, do-next that writes the draft in not just points at the old file, an attempted falsifier/key-focus before falling back to TO DECIDE) confirmed to hold on rebuild | critic 66.1k |

**Bar gap noted, not a defect:** M8 (verdict-from-fixed-set instrument check) prints the string `"steering"` in its own output regardless of which verdict the artifact actually states — round 2's critic independently confirmed the artifact's real verdict is "decorative" and that this is correct, but the instrument's diagnostic line appears to echo a template string rather than the matched value. Worth fixing in `bar-check.mjs` so the printed evidence doesn't mislead a future reader, even though it did not cause a false PASS here.
