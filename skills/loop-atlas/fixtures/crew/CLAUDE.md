# Rules

Every 30 minutes the loop wakes.
Pick the top item from docs/BACKLOG.md.
Implement the change with the minimum diff.
Run the tests; the definition of done is docs/DEFINITION-OF-DONE.md.
Append to docs/LOOP-STATUS.md, one line per tick.
Stop when the backlog is empty.
