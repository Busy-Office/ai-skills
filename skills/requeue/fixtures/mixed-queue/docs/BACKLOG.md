# Backlog

## Now
- [ ] Ship the CSV export endpoint — done when GET /export returns 200 and a parseable csv
- [ ] improve the dashboard
- [ ] Wire Stripe billing (blocked: needs decision on plan tiers)
- [ ] Rotate the production API key by hand
- [x] Wire the collector

## Later
- [ ] Ship the CSV export endpoint — done when GET /export returns 200 and a parseable csv
- [ ] consider refactoring the parser
